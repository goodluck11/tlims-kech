import {Ad} from '../ad';

export class Vehicle extends Ad {

  make: string;
  model: string;
  year: string;
  trim: string;
  color: string;
  transmission: any;
  mileage: string;
  registered: boolean;
  exchangeable: boolean;
}
