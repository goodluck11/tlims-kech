import {Ad} from '../ad';

export class Mobile extends Ad {
  model: string;
  storageCapacity: string;
  color: string;
  screenSize: string;
  ram: string;
  os: string;
  isExchangeable: boolean;
}
